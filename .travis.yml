language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t baczko/docker-react -f Dockerfile.dev .

script: 
  - docker run -e CI=true baczko/docker-react npm run test